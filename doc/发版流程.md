GitHub
  |
Docker
  |
Maven central

## 构建docker镜像

commit-tag-push


```shell
docker build -t etps:latest .
```

docker命令:

删除所有image
```shell
docker rmi -f $(docker images -aq)
```
进入容器
```shell
docker exec -it <容器名或容器ID> /bin/sh
```
